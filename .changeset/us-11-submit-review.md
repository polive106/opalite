---
"opalite": minor
---

Add review submission: approve, request changes, or comment on PRs from the terminal. Press `a` to approve or `x` to request changes from the DiffNav screen, with confirmation dialog and optional general comment.
